# Sử dụng Node.js phiên bản 18-alpine làm base image
FROM node:18-alpine

# Đặt thư mục làm việc trong container
WORKDIR /app

# Sao chép file package.json và package-lock.json vào container
COPY package*.json ./

# Cài đặt dependencies (npm install)
RUN npm install

# Cài đặt NestJS CLI toàn cục nếu cần
RUN npm install -g @nestjs/cli

# Sao chép toàn bộ mã nguồn vào container
COPY . .

# Cấp quyền thực thi cho lệnh "nest" (nếu cần)
RUN chmod +x /usr/local/bin/nest

# Build ứng dụng NestJS
RUN npm run build

# Mở port 5000 để container có thể nghe yêu cầu
EXPOSE 5000

# Chạy ứng dụng NestJS khi container khởi động
CMD ["npm", "run", "start:prod"]
